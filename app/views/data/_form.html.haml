= simple_form_for @datum, :url => @formURL do |f|
  = f.error_notification

  .form-inputs
    = f.association :datumType, label_method: :description, value_method: :id
    = f.input :value
    = f.input :comment

  .form-actions
    = f.button :submit
